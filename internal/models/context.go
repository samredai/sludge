package models

var USERNAME_KEY = int32(0)     // Key for retrieving the username set at init
var ACCOUNT_KEY = int32(1)      // Key for retrieving the account set at init
var APP_KEY = int32(2)          // Key for retrieving the tview.Application instance
var PAGE_HANDLER_KEY = int32(3) // Key for retrieving the tview.Pages instance
